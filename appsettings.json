{
  // ============================================================================
  // LOCATION — shared geographic area + time zone, used by EVERY feed below
  // ============================================================================
  // Nws, Spc, SpcMcd, Hwo, and the map-image bounding-box fallback all resolve
  // their WFO/centroid/geometry from these same Zones and Counties. If you add
  // a new alert feed in the future, it should read from here too, not carry its
  // own copy of Zones/Counties/TimeZone.
  "Location": {
    "Zones":    [],
    "Counties": [],
    "TimeZone": "America/Chicago"
  },

  // ============================================================================
  // POLLING — master loop cadence, drives every feed's check cycle
  // ============================================================================
  // Each feed below still self-gates on its own CheckIntervalSeconds; this only
  // controls how often the orchestrator ticks and checks whether any feed is due.
  // Only NWS alerts and SPC MCDs ever trigger the accelerated (ActiveAlert*)
  // window — SPC Outlook and HWO never do.
  "Polling": {
    "PollIntervalSeconds": 300,
    "ActiveAlertPollIntervalSeconds": 60,
    "ActiveAlertWindowHours": 4,
    "ActiveAlertMinSeverity": "Severe,Extreme"
  },

  // ============================================================================
  // NWS ALERTS FEED — the MASTER filter (server-side, hard gate)
  // ============================================================================
  // Regular NWS warnings/watches/advisories + Special Weather Statements (SPS).
  // Severity/Urgency/Certainty/EventTypes below are sent to api.weather.gov as
  // query parameters — they restrict what NWS returns in the first place.
  // Nothing downstream (per-platform MinSeverity, EventTypes) can "un-filter"
  // an alert excluded here; if it's not in this list, the bot never sees it.
  // This filter does NOT apply to SPC Outlooks, SPC MCDs, or HWO below —
  // those are separate feeds with their own severity values (see next section).
  // State is a fallback used ONLY if Location.Zones/Counties are both empty —
  // and ONLY for this feed. SPC/MCD/HWO always require explicit Zones/Counties.
  "Nws": {
    "State":              "MO",
    "Severity":           "Severe,Extreme",
    "Urgency":            "",
    "Certainty":          "",
    "EventTypes":         "",
    "AdditionalEventTypes": ""
  },

  // ============================================================================
  // SPC / TEXT-PRODUCT FEEDS — NOT gated by Nws.Severity above
  // ============================================================================
  // Each of these is a separate NWS/SPC data source with its own Enabled flag
  // and its own severity value assigned in code (not user-configurable here):
  //   Spc     (Convective Outlook) -> Severity computed from risk category
  //   SpcMcd  (Mesoscale Discussion) -> hardcoded "Severe"
  //   Hwo     (Hazardous Weather Outlook) -> hardcoded "Unknown" (no image, text only)
  // The ONLY severity gate that applies to these three is each platform's own
  // MinSeverity below — there is no server-side/master filter above them.
  "Spc": {
    "Enabled":              false,
    "CheckIntervalSeconds": 1800
  },

  "SpcMcd": {
    "Enabled":              false,
    "CheckIntervalSeconds": 300
  },

  "Hwo": {
    "Enabled":              false,
    "CheckIntervalSeconds": 300
  },

  // ============================================================================
  // DELIVERY PLATFORMS — where alerts from all feeds above get posted
  // ============================================================================
  // Every block below accepts the same three filter fields:
  //   MinSeverity  - optional extra restriction for THIS platform only. Blank ("")
  //                  means "accept everything that already passed the feed above."
  //   EventTypes   - optional event-name allowlist for THIS platform only.
  //   IncludeSpcOutlooks / IncludeSpcMcd / IncludeHwo - per-platform on/off switch
  //                  for each non-NWS feed (requires that feed's own Enabled: true
  //                  above). IncludeHwo defaults false since HWO is long-form text
  //                  best suited to a personal channel, not broad social platforms.
  "Facebook": {
    "Enabled": false,
    "PageAccessToken": "YOUR_NEVER_EXPIRING_PAGE_ACCESS_TOKEN",
    "PageId": "YOUR_PAGE_ID",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Instagram": {
    "Enabled": false,
    "PageAccessToken": "YOUR_NEVER_EXPIRING_PAGE_ACCESS_TOKEN",
    "InstagramAccountId": "YOUR_INSTAGRAM_ACCOUNT_ID",
    "ImageUrl": "https://yourserver.com/weather-alert-placeholder.jpg",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "X": {
    "Enabled": false,
    "ApiKey": "YOUR_API_KEY",
    "ApiSecret": "YOUR_API_SECRET",
    "AccessToken": "YOUR_ACCESS_TOKEN",
    "AccessTokenSecret": "YOUR_ACCESS_TOKEN_SECRET",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Bluesky": {
    "Enabled": false,
    "Handle": "yourhandle.bsky.social",
    "AppPassword": "YOUR_APP_PASSWORD",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Mastodon": {
    "Enabled": false,
    "InstanceUrl": "https://mastodon.social",
    "AccessToken": "YOUR_ACCESS_TOKEN",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Pushover": {
    "Enabled": false,
    "ApiToken": "YOUR_APP_API_TOKEN",
    "UserKey": "YOUR_USER_KEY",
    "DefaultPriority": 1,
    "ExtremePriority": 2,
    "EmergencyRetrySeconds": 60,
    "EmergencyExpireSeconds": 3600,
    "Sound": "siren",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Twilio": {
    "Enabled": false,
    "AccountSid": "YOUR_ACCOUNT_SID",
    "AuthToken": "YOUR_AUTH_TOKEN",
    "FromNumber": "+15551234567",
    "ToNumbers": [
      "+15559876543"
    ],
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "VoipMs": {
    "Enabled": false,
    "ApiUsername": "YOUR_API_USERNAME",
    "ApiPassword": "YOUR_API_PASSWORD",
    "Did": "5551234567",
    "ToNumbers": [
      "5559876543"
    ],
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Discord": {
    "Enabled": false,
    "WebhookUrls": [
      "YOUR_DISCORD_WEBHOOK_URL"
    ],
    "Username": "",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "DiscordDm": {
    "Enabled": false,
    "BotToken": "YOUR_BOT_TOKEN",
    "UserIds": [],
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Telegram": {
    "Enabled": false,
    "BotToken": "YOUR_BOT_TOKEN",
    "ChatId": "YOUR_CHAT_ID",
    "MinSeverity": "",
    "EventTypes": "",
    "IncludeSpcOutlooks": true,
    "IncludeSpcMcd": true,
    "IncludeHwo": false
  },

  "Map": {
    "Enabled": false,
    "AccessToken": "YOUR_MAPBOX_ACCESS_TOKEN",
    "Style": "mapbox/outdoors-v12",
    "Width": 600,
    "Height": 400
  }
}
