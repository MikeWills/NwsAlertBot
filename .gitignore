# Secret config override — never commit real credentials
appsettings.Local.json

# Build output
bin/
obj/

# Alert deduplication state (runtime data, not source)
posted_alerts.txt

# Startup platform confirmation tracking (runtime data, not source)
confirmed_platforms.txt

# Log files (rolling daily logs written by Serilog)
logs/

# Local setup notes
tailscale-setup.md

# IDE / OS
.vs/
*.user
.DS_Store
Thumbs.db
